// import * as router from 'react-router-dom'

import { describe, expect, it } from 'vitest'
// import { render, screen } from '@testing-library/react';

// import Landing from './Landing';

// vi.mock(`react-router-dom`, async () => ({ ...(await vi.importActual(`react-router-dom`)), useNavigate: ()=> vi.fn() } ));
// vi.mock(`react-router-dom`, async () => ({ ...(await vi.importActual(`react-redux`)), useSelector: ()=> vi.fn() } ));
// vi.mock(`react-router-dom`, async () => ({ ...(await vi.importActual(`react-router-dom`)), Link: ()=> vi.fn() } ));

describe('A truthy statement', () => {
    // beforeEach(() => {
    //     expect(router);
    // })

    it('should be equal to 2', () => {
        expect(1+1).toEqual(2)
    })



    // render(<Landing/>);
    
    // screen.debug();
})